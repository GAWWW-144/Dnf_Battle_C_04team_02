```mermaid
sequenceDiagram
    actor Player as 플레이어
    participant 캐릭터생성화면
    participant 전투
    participant 플레이어클래스 as 플레이어
    participant 전사클래스 as 전사
    participant 마법사클래스 as 마법사

    Player->>캐릭터생성화면: 폼 입력 (플레이어아이디, 캐릭터명, 직업, 레벨)
    캐릭터생성화면->>캐릭터생성화면: 입력_캐릭터_정보() 실행
    캐릭터생성화면->>전투: 캐릭터생성(플레이어아이디, 캐릭터명, 직업, 레벨)
    
    전투->>플레이어클래스: 플레이어체크(플레이어아이디) <<include>>
    플레이어클래스-->>전투: boolean 반환 (true/false)

    alt 플레이어체크 == true ("hero"인 경우)
        alt 직업 == "전사"
            전투->>전사클래스: 생성자 호출(캐릭터명, 레벨) 및 속성(체력, 공격력) 할당
            전사클래스-->>전투: 생성된 전사 객체 반환
        else 직업 == "마법사"
            전투->>마법사클래스: 생성자 호출(캐릭터명, 레벨) 및 속성(체력, 공격력) 할당
            마법사클래스-->>전투: 생성된 마법사 객체 반환
        end
        전투->>플레이어클래스: 캐릭터설정(생성된캐릭터)
        전투-->>캐릭터생성화면: 캐릭터 객체 반환
    else 플레이어체크 == false
        전투-->>캐릭터생성화면: null 또는 생성 실패 반환
    end

    캐릭터생성화면-->>Player: 캐릭터 생성 결과 출력
```