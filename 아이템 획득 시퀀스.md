```mermaid
sequenceDiagram
    actor Player as 플레이어
    participant UI as Add_Item_UI
    participant 전투 as 전투
    participant 플레이어객체 as 플레이어
    participant 캐릭터 as 캐릭터
    participant 인벤토리 as 인벤토리
    participant 아이템 as 아이템

    Player->>UI: 아이템 정보 입력 (플레이어id, 이름, 타입, 가치)
    UI->>전투: 아이템획득(플레이어id, 캐릭터, 이름, 타입, 가치)
    
    전투->>플레이어객체: 플레이어체크(플레이어id) <<include>>
    플레이어객체-->>전투: true 반환

    alt 플레이어체크 통과 && 캐릭터 보유
        전투->>아이템: 생성(이름, 타입, 가치) 및 등급부여
        아이템-->>전투: 아이템 객체
        전투->>캐릭터: get인벤토리()
        캐릭터-->>전투: 인벤토리 객체
        전투->>인벤토리: 아이템추가(아이템 객체)
        
        alt 인벤토리.size() < 10
            인벤토리-->>전투: true (추가 성공)
            전투-->>UI: "획득 성공" 메시지 반환
        else 인벤토리 가득 참
            인벤토리-->>전투: false (추가 실패)
            전투-->>UI: "인벤토리 공간 부족" 메시지 반환
        end
    else 플레이어 불일치 또는 캐릭터 없음
        전투-->>UI: "권한 없음/캐릭터 없음" 에러 반환
    end
    
    UI-->>Player: 아이템 획득 결과 출력
